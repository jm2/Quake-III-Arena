# Native complete reply dictionary — 2026-09-18

BotLoadReplyChat dereferenced unchecked persistent reply/key/string/message
imports, concatenated arbitrary bot names into a 256-byte stack buffer, returned
prefix data after sticky source errors and published priorities that public
selection could not convert to its native integer. Use checked owned heap
records, bound complete name lists including separators/NUL, reject source
errors and require finite priorities representable by selection's integer.
Release every candidate piece/key/string/message/reply and the source on failure;
no failed candidate consumes replacement persistent hunk. Existing normal
free/shutdown physically releases successful private records.

Keep native reverse key/dictionary/message order, every special/prefixed/string/
match key, combined-name separator, ordinary fractional priorities, optional
empty files and zero-message dictionaries. Public reply capture/construction and
line timing remain. Commercial 1.32c imports/syscalls and retail grammar are unchanged.

## Host and product evidence

- [The actual factory fixture](../tests/bot_reply_dictionary_regression.c) runs
  whole chat/parser/source/allocator bodies with physical engine-owner imports.
- 198 source/container/key/string/piece/message nullable positions across fresh/
  prior dictionaries reject, release all candidates/source owners, preserve prior
  header/key/line pointers and timing bytes plus public selection, then retry.
- Exact 255-byte single/combined name lists remain complete. One-byte-over and
  larger lists reject. Unterminated/invalid pieces, suffix lexical/directive/
  syntax errors and priorities at/rounding to 2^31 reject. Ten families reproduce
  120 failures against the unchanged pre-fix body. Twelve separate original
  native golden runs preserve every key branch, order, fractional/empty data,
  names and actual reply selection/capture/construction/timing.
- All six normal/fast/debug/debug-fast/manager/manager-fast modes pass Clang
  ASan/UBSan/float-cast-overflow and optimized GCC. CI runs both compilers. Parent
  piece tests, five ledger/manifest checks, Bash syntax and diff checks pass.

| Product | PEF bytes | SHA-256 |
| --- | ---: | --- |
| Quake3 | 3,792,573 | `22844c81798182b9fcbf9ad654413c829a16682bea486ef5041352f7e81d9919` |
| Quake3_TeamArena | 3,941,147 | `c2953935b28d37a2c99f1aae29bc84f60c0e4dc894e4f5c2916bcb6a5a0a6cc5` |

Both PPC products have valid PEF headers and build without diagnostics using
[the recorded toolchain libraries](qvm-loading-validation.md).

## Remaining acceptance

Keep #48 open for initial dictionary/cache factories, complete library/world
publication and aggregate parser/work/memory budgets. Optional empty data and
failed loading require distinct setup status in complete library work. Physical
hunk reset remains engine-owned. Retail/Mac OS 9 execution is explicitly deferred.
