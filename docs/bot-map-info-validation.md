# Complete native map metadata — 2026-09-18

BotInitInfoEntities releases prior locations/camps before unchecked nullable
allocations. Public level initialization can also replace its pool before a later
metadata failure. Stage the complete cleared pool/free chain and metadata lists
privately, then publish their roots together. Failure reports an existing map-API
error, frees private heap owners and retains every prior root/payload byte.

Keep native BSP epair fields, location/camp reverse order, solid-camp omission and
diagnostics. Private metadata initialization replaces only its own two lists; its
pool stays independent. Retail void BotInitLevelItems export/syscall and commercial
1.32c data/protocol interfaces remain. The checked engine map API and game failure
handling from the preceding pool step remain in use. Restart reads the engine
readiness flag through the unchanged variable syscall after game DLL/QVM data
reset; failed map status cannot be revived by an ordinary stale variable. Legacy
engines that return an empty unknown-variable value retain normal restart behavior.

## Validation

Sixteen original private/public actual-body nullable allocation proofs fail in
normal/fast modes with empty/occupied prior state. Six preceding pool-failure
checks and two original native metadata field/order goldens pass separately; they
are retained guards, not newly demonstrated defects. The proof renames the old
void private initializer and supplies a return-true adapter for its new private
status signature. Original metadata body and public caller behavior remain.

Fixed tests execute 36 nullable failure stages across private metadata, retail
void initialization and checked initialization. They snapshot full prior location,
camp and both pool nodes, all metadata/free/active roots and the active count.
Only complete native shared count-cache variables survive failure; private pool/
list owners physically release. Each case retries and checks names, origins,
areas, camp scalar fields, reverse order and native solid-camp omission. Private
success preserves an independent occupied pool; public success replaces all roots.

Fourteen actual Export_BotLibLoadMap failure/retry cases check propagated status,
retained prior roots, skipped brush/success handling and native retry order. Three
private/retail/checked complete goldens retain native fields/diagnostics. Every
physical heap owner and tracked logical record releases at teardown. The fixture
uses actual goal, source, structure, variable, allocator and engine API bodies,
five synthetic BSP entities and an absent item configuration; runtime navigation
queries assert unreachable. This is metadata/pool evidence, not a complete world
or item-entity scan transaction.

Clang ASan/UBSan/float-cast-overflow and optimized GCC pass six normal/fast/debug/
tracked modes without diagnostics. The preceding actual pool/map-API fixtures
also pass six modes with both compilers. After integrating the game failure fix,
metadata and game fixtures pass both compilers again; game tests cover four
base/missionpack normal/fast modes. Five ledger/manifest checks, Bash syntax and
diff checks pass; GCC/Clang CI runs the metadata fixture.

Both PPC products build with zero diagnostics and valid PEF headers:

| Product | PEF bytes | SHA-256 |
| --- | ---: | --- |
| Quake3 | 3,783,927 | `4b765efac5aa77a1c10605bec032b9c880c48274d250e5a06d7ab961ebb96826` |
| Quake3_TeamArena | 3,932,501 | `d56d822d051d4a0e53703ed64d797ba75386e8b1612c6dba426226433f219793` |

Temporary toolchain libraries: [loading evidence](qvm-loading-validation.md).
Host callback alignment uses measured native ownership prefixes as documented in
[item evidence](bot-item-config-validation.md).

## Remaining acceptance

Keep #47/#48 open for later item/entity-scan failure policy, complete AAS/world/
library replacement, weapon/chat consumers and aggregate memory/parser budgets.
Heap release here is physical; hunk arena reset remains an engine-owned boundary.
Retail assets, Mac OS 9 execution and target bot acceptance remain deferred.
