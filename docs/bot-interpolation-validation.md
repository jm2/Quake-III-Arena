# Native character interpolation owners — 2026-09-18

Character interpolation dereferences nullable output/string imports and publishes
its handle before string cloning completes. Stage the output privately, reject a
missing header, and release every earlier output string/header after a failed
clone. Mark each string owned only after its copy succeeds. Publish the handle
after all fields complete. Original characters remain untouched.

Retain native float interpolation, lower-character integer/string choices,
mixed/unset-field behavior, filename/skill metadata and first-free handles.
Commercial 1.32c assets, parser/QVM/syscall interfaces and structure layouts are
unchanged. This step checks owners; numeric/skill validation is separate work.

## Validation

The runner links actual character/preprocessor/lexer/libvar/Q_shared bodies;
file/heap/printing are interfaces. Inputs come from real two-skill source text.
Native interpolation, registry publication, public getters and physical cleanup
execute production bodies. An import observer checks that the prospective handle
remains empty at every allocation.

Four original actual-body proofs fail: a null output header, both null string
copies and publication before cloning completes. A separate original-body run
passes the same ordinary native value/handle/cleanup goldens as the fixed body.

Four desired skills retain literal native float values, lower integer choices
including mixed types, unset fields and independent ordinary/empty strings.
Every output/header and both string imports fail independently, release all
prospective physical owners, preserve both complete input structures/string
contents and leave the handle empty. Every failure retries into the same native
free slot. Public numeric/string getters return complete results. Invalid/missing
handles and a full native table reject before imports. Completed output and input
cleanup finally return every physical owner/token counter to zero.

Clang normal/release fast-math ASan/UBSan, optimized GCC and parent real character
sanitizers pass. GCC and Clang CI execute both configurations. Whole-preprocessor
host compilation retains two existing unrelated `abs(long)` expression warnings.
Five ledger/manifest checks, Bash syntax and diff checks pass. Both Retro68
products rebuild with zero compiler diagnostics and valid PPC PEFs:

| Product | PEF bytes | SHA-256 |
| --- | ---: | --- |
| Quake3 | 3,766,521 | `d2ff77776a8dd3ff92ce83bd2561917d96c4269b3facb461bc080c6b9a7c2cd1` |
| Quake3_TeamArena | 3,915,095 | `9b33d503db28162c801dfd11c3ecc4b9a0ef138518bd94ebd8f9c449cbf7f427` |

Temporary toolchain libraries: [loading evidence](qvm-loading-validation.md).
Source includes the checked registry/source/character/macro/include/builtin/
libvar/routing parents.

## Remaining acceptance

Keep #48 open for default inheritance, character numeric/skill bounds, remaining
directive/global/indent/unread-token factories, lexical cursors, further real
nested/malformed sources and aggregate parse/recursion budgets. Retail/Mac OS 9
execution remains deferred.
